# Models Directory
# ================
# Place your trained .pt model files here:
#
# 1. violation_model.pt  — Traffic violation detection (Model 1)
#    Train using: training/train_violation_model.py on Kaggle
#
# 2. plate_model.pt      — License plate detection (Model 2)
#    Train using: training/train_plate_ocr_model.py on Kaggle
#
# The system will auto-detect these files on startup.
# If violation_model.pt is missing, it falls back to ../best.pt
