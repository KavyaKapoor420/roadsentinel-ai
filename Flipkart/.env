# Leave empty in dev — Vite proxies /api → https://fgr2-backend.mooo.com (see vite.config.ts)
VITE_API_BASE_URL=
# Leave empty in dev — uses ws://localhost:8080/api/analyze/stream via the same proxy
VITE_WS_URL=
